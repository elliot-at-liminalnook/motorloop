CAPI=2:
# SPDX-License-Identifier: MIT
name: motorloop:ip:divider32:0.1.0
description: "32-bit sequential divider"
filesets:
  rtl:
    files:
      - ../rtl/divider32.v
    file_type: verilogSource
targets:
  default: &default
    filesets: [rtl]
    toplevel: divider32
  lint:
    <<: *default
    default_tool: verilator
    tools:
      verilator:
        mode: lint-only
